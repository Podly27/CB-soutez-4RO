Options -Indexes

RewriteEngine On

# Serve existing files/directories from root as-is
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise forward everything into /public
RewriteRule ^(.*)$ public/$1 [L]
